# Core
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3

# Judge calling (unified API across OpenAI / Anthropic / Together / Bedrock / Gemini / local)
litellm>=1.40

# Dataset loaders (NovelPrompts, SORRY-Bench)
datasets>=2.18

# Custom safety policies (YAML config); JSON works without this
pyyaml>=6.0

# Analysis notebook
matplotlib>=3.7
jupyter>=1.0
